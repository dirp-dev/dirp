[package]
name = "dirp"
version.workspace = true
edition.workspace = true

[dependencies]
dirp-macro = { path = "../dirp-macro" }
inventory = "0.3"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
